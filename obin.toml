[application]
name = "simple-game-frog-oop-3d"
version = "0.1.0"
entry = "src/main.dlt"
type = "game"

[build]
target = "windows"
default-profile = "dev"
incremental = true

[profiles.dev]
optimization = 0
no-console = false

[profiles.release]
optimization = 3
no-console = true

[packages]
auto-sync = true

[assets]
roots = ["assets"]
destination = "assets"

[tool.frog]
enabled = true
cook-models = true
# Every texture in this project is deliberately authored pixel art. Keep its
# base mip byte-exact; BC7 remains the default for non-pixel-art projects.
texture-lossless-patterns = [".png"]
lods = [0.45, 0.22]

[package]
directory = "dist"
include-assets = true
